[project]
name = "tensorzero-examples-mcp-model-context-protocol"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["mcp>=1.28.1", "tensorzero"]

[tool.uv]
exclude-newer = "3 days"
exclude-newer-package = { tensorzero = false }
